version: 0.2

phases:
  install:
    commands:
      - echo "Installing dependencies if any"
  build:
    commands:
      - echo "Zipping script.py"
      - zip function.zip script.py
artifacts:
  files:
    - function.zip
